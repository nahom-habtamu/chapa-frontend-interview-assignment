// Re-export common types for backward compatibility
export {
  type ApiError, type AuthResponse, type AuthState, type AuthToken,
  type LoginCredentials,
  type RegisterCredentials, type BaseUser as User, type UserRole
} from "../common/types";
